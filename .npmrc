# Default registry
registry=https://registry.npmjs.org/

# Ensure package-lock uses exact versions
save-exact=true

# Log level for npm operations
loglevel=warn

# Don't include optional dependencies by default
optional=false

# Authentication token is set via environment variable or npm login
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}